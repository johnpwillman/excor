<div class="row">
  <% if user_signed_in? %>
  <div class="col-md-4">

    <h3>Start your own Exquisite Corpse</h3>
    <!--<%= "Display current user if logged in: " + current_user.email %>-->
    <%= form_for [current_user, @poem], :html => {:class => 'form-horizontal', :role => 'form'} do |f| %>
    <div class="form-group">
      <%= f.label :title, :class => 'col-sm-2 control-label' %>
      <div class='col-sm-10'><%= f.text_field :title, :class => 'form-control' %></div>
    </div>
    <div class="form-group">
      <%= f.label :first_line, :class => 'col-sm-2 control-label' %>
      <div class='col-sm-10'><%= f.text_area :firstline, :class => 'form-control' %></div>
    </div>
    <div class="col-sm-9">
    </div>
    <div class="col-sm-3">
      <%= f.submit 'submit', :class => 'btn btn-default' %>
    </div>
    <% end %>
  </div>


  <% else %>
  <div class="col-md-4">
    <p>Please log in to contribute!</p>
  </div>
  <% end %>

  <div class="col-md-8">
    
    <table class="table">
      <%= render "layouts/poem" %>
    </table>
    
  </div>
  
</div>